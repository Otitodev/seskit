"""Amazon SES provider for SESKit.

Implemented in Phase 4 (see SESKit_MVP.md §31). This package exists now so the
workspace layout and package boundaries are established: provider-specific
logic stays isolated here and never leaks into the API or core packages
(§32.8).
"""

__all__: list[str] = []
